# OMC local state
.omc/

# Claude local settings
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Node (root-level safety net)
node_modules/

# Env
.env
.env.*

# Secrets — storage state de login (NUNCA commitar)
auth.json

# Playwright CLI — artefatos de automação (snapshots, console logs)
.playwright-cli/
